// ---- Solution Jenkinsfile for the DevOps Workshop sample app ----
// Place this file at the ROOT of your sample-app repository
// (i.e. sample-app/Jenkinsfile), not inside solution/.

pipeline {
    agent any

    environment {
        IMAGE_NAME = "devops-workshop-app"
        IMAGE_TAG  = "build-${BUILD_NUMBER}"
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                dir('sample-app'){
                    sh 'npm install'
                }
            }     
        }

        stage('Test') {
           steps {
                dir('sample-app'){
                    sh 'npm test'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                sh "docker build -t devops-workshop-app:latest sample-app/"
            }
        }

        stage('Smoke Test the Image') {
    steps {
        // We use --network host so localhost aligns perfectly inside the container environment
        sh "docker run -d --name smoke-${BUILD_NUMBER} --network host devops-workshop-app:latest"
        sh "sleep 5" 
        sh "curl -f http://localhost:3000/health" // Notice we use 3000 here because it's native port
    }
}

        stage('Push Image') {
            when {
                branch 'main'
            }
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'dockerhub-credentials',
                    usernameVariable: 'DOCKERHUB_USER',
                    passwordVariable: 'DOCKERHUB_PASS'
                )]) {
                    sh """
                        echo "\$DOCKERHUB_PASS" | docker login -u "\$DOCKERHUB_USER" --password-stdin
                        docker tag ${IMAGE_NAME}:${IMAGE_TAG} \$DOCKERHUB_USER/${IMAGE_NAME}:${IMAGE_TAG}
                        docker push \$DOCKERHUB_USER/${IMAGE_NAME}:${IMAGE_TAG}
                    """
                }
            }
        }
    }

    post {
        success {
            echo "Pipeline succeeded: ${IMAGE_NAME}:${IMAGE_TAG}"
        }
        failure {
            echo "Pipeline failed — check the stage logs above."
        }
        always {
            sh "docker rm -f smoke-${BUILD_NUMBER} || true"
        }
    }
}
